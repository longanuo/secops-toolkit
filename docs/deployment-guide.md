# Deployment Guide

This document describes how to deploy the SecOps Toolbox in various environments.

## Docker Deployment
Refer to the `Makefile` targets:
- `make docker`
- `make docker-up`

## Local Python Deployment
Install the package locally:
```bash
pip install -e .
```
